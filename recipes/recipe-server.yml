---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: aurora-server
description: This is my personal OS image.

base-image: ghcr.io/ublue-os/aurora
image-version: stable

modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - from-file: modules/local-user-backup.yml
  - from-file: modules/sonic.yml
  - from-file: modules/thinlinc.yml
  - from-file: modules/shared-folder.yml
  - from-file: modules/vikunja.yml
  - from-file: modules/printers.yml
  - from-file: modules/flatpak.yml
  - from-file: modules/network.yml

  - type: rpm-ostree
    repos: []
    install:
      - qpdf
      - konsole
      - yakuake
      - inotify-tools
      - pam-kwallet
    remove:
      - kdeconnectd
      - kde-connect-libs
      - kde-connect
      - input-remapper
      - kdebugsettings

  - type: script
    scripts:
      - shared/konsole.sh
  - type: script
    scripts:
      - server/uivision.sh
  - type: script
    scripts:
      - server/kde-config.sh
  - type: script
    scripts:
      - shared/bluebuild-cleanup.sh

  - type: systemd
    system:
      enabled: []
        #- restic-backup.service
        #- restic-backup.timer
      disabled:
        - sleep.target
        - suspend.target
        - hibernate.target
        - hybrid-sleep.target

  - type: signing
